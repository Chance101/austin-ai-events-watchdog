{
  "name": "austin-ai-events-watchdog",
  "version": "0.1.0",
  "description": "External ground-truth watchdog for austinai.events — runs on its own schedule, outside the main repo's modification scope, to catch dead-system and confident-lobotomy failure modes.",
  "type": "module",
  "main": "scripts/run.js",
  "scripts": {
    "start": "node scripts/run.js",
    "liveness": "node scripts/liveness.js",
    "coverage": "node scripts/coverage.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.1",
    "@supabase/supabase-js": "^2.47.0",
    "cheerio": "^1.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
